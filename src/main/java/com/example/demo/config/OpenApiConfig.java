package com.example.demo.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class OpenApiConfig {
    // Rely on springdoc-openapi auto-configuration.
    // To customize OpenAPI metadata, add properties to application.properties or implement a bean that uses
    // io.swagger.v3.oas.models.OpenAPI. The previous implementation imported io.swagger classes that
    // weren't resolvable in the current environment; keep this placeholder to avoid compilation errors.
}
